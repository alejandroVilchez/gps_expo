import React from "react";
import {Text, View} from "react-native";

export default function soundSettings(){
    return (
        <View style={{flex: 1, alignContent: "center"}}>
            <Text>Under construction :P</Text>
        </View>
    );
}
